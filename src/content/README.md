# Content Folder

This folder contains all the articles and metadata for the blog.

- `articles.ts`: The main data file that lists all articles shown on the homepage.
- `posts/`: A directory where you can store individual markdown files for each article.

To add a new article:
1. Create a new `.md` file in `posts/`.
2. Add an entry to the `articles` array in `articles.ts`.
